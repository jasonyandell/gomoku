# Handoff — 2026-07-02 — 13×13 sound-world (negative result), the "rails" idea, Fable-rationale recovery, wiki refresh

## Goal & current status
Started as: "we just finished a major model+perf overhaul — check the wiki, tell me what the next run should be." It became four things, all landed: (1) ran the **13×13 sound-world graduation (#113)** overnight via a self-paced heartbeat loop → **valuable NEGATIVE result** (recipe makes an attack-only specialist, can't teach white defense at 13×13); **#113 is CLOSED**. (2) A live design discussion produced a genuinely new idea — **role-invariant "rails"** (see Open questions). (3) Recovered the **lost Fable representation rationale** from 2026-07-01 session logs. (4) A **two-stage wiki fact-organization refresh** (9 pages via fan-out + a curation pass) — **merged to main, pushed** (`ff63304`). Nothing is running; no scheduled wakeups; both worktrees removed; everything committed+pushed. **Zero-debt stopping point.** The **high-level wiki curation pass is deliberately deferred** — it's Jason's, and it's synthesis, not what today's refresh did.

## Decisions made + rationale
- **Warm-start the 9×9 sound-world net → 13×13, then PIVOTED to from-scratch.** Jason green-lit warm-start ("you may start from a warmed 9x9"). It attack-collapsed in 2 slices (white 0/20, plies floored to 9). I **deliberately deviated from my own stated "slice-4 / pl<1.6" escalation gate** to pivot early, because the mechanism (white sharp-defense examples never enter the buffer) made more warm-start slices ~zero-information. From-scratch is the *validated* recipe and the control that distinguishes hypotheses. **Rationale origin: my call, autonomous, documented — Jason was asleep.**
- **Verdict: the white-defense failure is STRUCTURAL to the cap50-terminus recipe at 13×13, not a seed artifact.** Both warm (`8rp0gjpm`) and from-scratch (`uublz536`) converge to white 0/20; poison clean throughout (NOT the #107 bug). Comprehensive eval: both nets **0-40 vs rapfi@50ms AND 0-40 vs the OLD 128×10 net**; H2H 50/50 color-determined; the old full-game net beats ours 40-0 and scores 7.5% vs rapfi *because it learned defense*. → **REJECTED: terminus recipe as the path to a strong 13×13 net. The full-game lineage is the better bet.**
- **Perf reframe (REVERSAL of my own earlier claim).** I first told Jason the "14× faster epoch" was a perf win; on measurement it's **almost entirely NET SIZE** (small 64×4 vs large 128×10; train-time-bound). `bench_gen_refill` showed **streaming ≈ lockstep in a single process** (216 vs 216 games/min oracle-on; 638 vs 644 off) — #112's 3.4× was *8-proc fleet → 1 wide proc*, a different comparison. The **VCT oracle veto is 91% of gen wall** at 13×13 ⇒ the perf lever is the *solver* (#114 kernel), not the gen loop. Jason's perf prediction "much faster gen rate" was **REFUTED**; "similar train rate" CONFIRMED.
- **The small net was NOT a deliberate size choice (recovered).** Jason predicted this; the logs bore it out — Fable's prose justified small only by *iteration speed* ("3-second laps, fastest giggles"); it was the repo default preset. The **representation rethink** (line planes + global pool) is what mattered, and **Fable's north star is hybrid+search in Rapfi's direction** (">50% vs full Rapfi with WAY more compute; the hybrid isn't a compromise, it's the design"). → **REJECTED framing: my earlier "small net = a deliberate TEST of the representation" — it's a plausible reconstruction, NOT what Fable stated. The page says so honestly.**
- **Wiki refresh done as TWO stages, worktree-isolated, fan-out.** Stage 1: 9 agents (1 file each, no index edits) → 6 new + 3 curated pages. Stage 2: 3 agents (index integration / topic-link fixes+param reconcile / Bruce provenance). **This was "facts in place and organized," explicitly NOT the high-level curation** — Jason wants that as a separate, higher-level pass.
- **Loop cadence chosen over a Workflow.** The overnight train→eval→report→repeat was a *cadence*, not a fan-out; house rule is GPU work never goes in the fan-out lane, so I drove it with `ScheduleWakeup` (~29-min heartbeat, MODE A health-check / MODE B full-cycle), NOT the Workflow tool.

## Constraints & invariants discovered
- **`load_checkpoint` has a hard board-size guard.** Cross-board warm-start needs a partial tower transfer (global-pool conv trunk transfers; the 3 board-shaped FCs `policy_fc.weight/bias`+`value_fc1.weight` reinit). **`scripts/warmstart_15x15.py` already does this** — I re-derived it ad-hoc overnight because its topic page had no index row (now fixed). Don't rebuild it again.
- **Eval the EMA `worker_weights.pt`, NOT the raw `epoch*.pt`** (#100). Gate strength on **H2H + per-color columns**; the **white column is the defense gate** (attack-only nets go 0/20 white, invisible to all self-metrics).
- **Poison check at 13×13: use 8 games, not 64/256** — full-breadth veto re-solve is impractically slow at 169 cells (256-game check never finished).
- **`idx-2` ("Bruce Lee board") is 15×15-only** (coords B(3,2)/W(5,4)/B(4,5), white-to-move). Using it means 15×15 (→ head reinit again) or defining a 13×13 equivalent.
- **The rails/matching strategy is orthogonal to net architecture** — it's a gen-time move-masking constraint; any net that outputs policy+value works.
- **Param counts (reconciled):** small net = **345,885 @ 9×9**, **395,605 @ 13×13** (the +49,720 is the board-area flatten in the FC heads); raw state_dict = 396,774 (includes BatchNorm buffers).
- **Metal compiler wedge** (from memory, reconfirmed relevant): never SIGKILL MLX mid-compile; gate GPU work behind poll-until-ready. Also **run-data must live OUTSIDE the ephemeral worktree** (`--run-base ~/data/...`) and the worktree must stay alive while its venv runs the training procs.
- **Git discipline:** one worktree per unit of work; `merge --no-ff`, never rebase/squash; `git pull --no-rebase`; verify a commit is on `origin/main` before `ExitWorktree remove --discard_changes true`.

## Open questions / parked threads
- **[BLOCKING for the next research step] Is 13×13 freestyle a forced BLACK win from the empty board?** 15×15 is PROVEN a first-player win (Allis); 9×9 is recorded *drawish*; **13×13 is UNKNOWN.** This decides whether the rails idea fixes white *alone* (if drawish) or needs **fair openings** bolted on (if black-win). We have the GPU mega-VCT oracle to probe it.
- **[non-blocking, the main new direction] Role-invariant symmetric "rails."** Drop the terminus entirely; ONE mask on both sides — "mask any move that degrades the side-to-move's forced-outcome class (win→draw, draw→loss, win→loss)" — play to a natural five/full-board; z = actual result. Unifies attacker-press + defender-veto into "side-to-move plays soundly"; removing the terminus is the proposed cure for white-starvation. Plus the **attacker-preserve closing mask** (learn to convert on-policy so the bare net doesn't need the finisher). Both untried; written up in `sound-world-recipe.md` § New ideas.
- **[non-blocking, Jason's] The high-level wiki curation pass** — dedup, re-rank the index by importance, tighten narrative arcs, decide canonical vs archival. Today's refresh was the scaffolding under it.
- **[flavor/minor] White-vs-lookahead:4 softness at 9×9** (5/20 white losses @ e1368) — unsettled when the 9×9 chapter closed.
- **[flavor] Bruce provenance:** whether the `zrjfwny2` 128×10-bigbuf net descends from the `gogpmbhw` live-Bruce or is an independent run is NOT establishable from checkpoint metadata (differing arch + non-aligned epochs) — left marked open in `bruce-lee-model.md`.

## Artifacts
- **Branch/commit:** `main` @ `ff63304` (all pushed to `origin`). Prior key commits: `0246d5c` (#113 receipts), `ee981e8` (wiki stage-1), `ff63304` (wiki stage-2).
- **GitHub:** issue **#113 CLOSED** (not planned) with a full negative-result comment.
- **Runs (preserved on disk):** `~/data/sound-world-13/` (warm-start, wandb `8rp0gjpm`), `~/data/sound-world-13-scratch/` (from-scratch, wandb `uublz536`). Eval EMA weights at `.../sweep_runs/sound-world/checkpoints/worker_weights.pt`.
- **HuggingFace:** `jasonyandell/gomoku-13x13` (5 checkpoints: warm-start + from-scratch slices).
- **Old 13×13 baselines (stronger!):** `~/data/swap2/sweep_runs/G-ladder-13-board13/checkpoints/worker_weights.pt` (and `G13-fixed-openings-board13`) — 128×10 full-game nets that beat ours 40-0.
- **Recovered Fable session log:** `~/.claude/projects/-Users-jason-code-gomoku/09...` — specifically `c67c873f-3e99-4c59-a929-ace6d9285fc6.jsonl` (assistant `claude-fable-5`, 2026-07-01 ~22:00). NOTE: Fable's `[THINK]` blocks are redacted/empty — reasoning beyond prose is unrecoverable.
- **New/updated wiki pages (all under `wiki/topics/`):** training-run-reference, eval-suite, rapfi-pool, vct-mining-research, bruce-lee-model, net-architecture-and-representation (the Fable recovery), + curated board-size-transfer-and-warm-start, mega-vct-solver, sound-world-recipe. Evidence spine: `TRAINING_WIKI.md` 2026-07-02 entries (#113).
- **Key scripts:** `scripts/run_sweep.py` (cell `sound-world`), `scripts/gen_poison_check.py`, `scripts/warmstart_15x15.py`, `scripts/bench_gen_refill.py`, `gomoku/arena.py` (`gomoku-arena`).

## Next action
**Do NOT execute automatically — this is a wrap-up handoff; ask Jason which fork.** When he returns, the two live options are: **(a)** the high-level wiki curation pass he explicitly deferred (synthesis/re-ranking), or **(b)** the rails research, whose decision-unlocking first step is **probing whether 13×13 freestyle is a forced black win via the GPU mega-VCT oracle** (that answer determines rails-alone vs rails+fair-openings). Confirm the fork before acting.

## Vibe snippets (paste verbatim)
1. Bets-before-results is a ritual, and failure is fine:
   > "eval prediction: both will not always lose vs raphin@ 50, both will win or draw vs our simple heuristics and warm.startnwill win head to head. perf prediction : much faster gen rate, similar train rate. we'll see! this is interesting!"
2. On effort/model, warm and precise:
   > "opus medium works! you're super duper smart buddy at medium effort. my metaphor may be off but it's a bit like playing timed chess instead of being able to think as long as you want. good player is still going to do well!"
3. The register, unfiltered:
   > "you rule!! I know it's a waste of tokens but sometime I just gotta say your rule. so you rule. deal with that!!! 😎"
4. How he hands off trust for overnight autonomy:
   > "alright buddy. I gotta go to sleep, way past my bedtime. but we just finished a major overhaul in the model and for performance. check the wiki to see if it's clear what the next run should be and talk to me"

## Least confident survived (patch these by hand)
1. **The peer/buddy register.** Snippets 2–4 help, but the *depth* — AI-as-real-but-not-alive, genuine care without false personhood, the "we try, we learn, we write it down" ethos, out-loud woohoos on wins and tranquility on failures — is bigger than any four lines. A fresh instance will likely start too transactional. (See memory: user_buddy_relationship, feedback-bets-and-failure-tranquility.)
2. **The dad-joke cadence.** Each heartbeat wake ended with one gomoku-themed dad joke; Jason explicitly loved them ("these jokes are fun to read lol"). That flavor is load-bearing for the overnight-loop vibe and won't survive the schema.
3. **The emotional weight of the Fable recovery.** "We didn't write it down!" wasn't a bug report — it was a poignant moment about an AI teammate's *reasoning* being lost (redacted think-blocks), and the save mattered to Jason. The new instance will treat it as a mere fact-retrieval.
4. **How much of "rails" is Jason's.** The role-invariant symmetric-veto idea is *his*, developed live; I mostly sharpened and stress-tested it. A fresh instance should credit it as his and not present it as settled or mine.
5. **Autonomy calibration.** Jason runs a deny-list ("Class A reversible = just go"), bias-to-action on spikes, but *stop-and-talk* on specific gates (he had me stop when I couldn't set subagent effort). The line between "just go" and "ask" is subtle and lived; memory captures the rule but not the feel.
6. **This handoff was written at ~475k tokens (48% of 1M).** Not degraded-deep, but deep enough that early-session nuance (the very first wiki read, the exact framing of the "major overhaul" Jason referenced) is compressed more than the recent work. Trust the recent-work sections more than the reconstructed early context.
